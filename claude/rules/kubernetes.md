---
description: kubectl command conventions
globs:
  - "**/k8s/**"
  - "**/*.yaml"
  - "**/*.yml"
---

# Kubernetes

Always specify `--context` and `--namespace` (or `-n`) when executing kubectl commands.
